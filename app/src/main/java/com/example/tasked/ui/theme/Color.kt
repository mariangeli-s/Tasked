package com.example.tasked.ui.theme

import androidx.compose.ui.graphics.Color

//Paleta de Colores para "Tasked"

// Colores Primarios (utilizados en barras de app, botones principales)
val PrimaryBlue = Color(0xFF42A5F5) // Un azul vibrante pero calmante
val LightBlue = Color(0xFF80D8FF) // Un azul más claro para acentos
val DarkBlue = Color(0xFF1976D2)  // Un azul oscuro para contraste

// Colores Secundarios (para Floating Action Button, elementos interactivos secundarios)
val AccentGreen = Color(0xFF66BB6A) // Un verde amigable y de progreso
val LightGreen = Color(0xFF98EE99) // Un verde más claro
val DarkGreen = Color(0xFF388E3C)  // Un verde oscuro

// Colores Terciarios (para acentos sutiles, detalles)
val SoftPurple = Color(0xFFCE93D8) // Un morado suave
val LightPurple = Color(0xFFE1BEE7)
val DarkPurple = Color(0xFF9C27B0)

// Colores de Fondo y Superficie (para la base de las pantallas y las tarjetas/elementos)
val BackgroundLight = Color(0xFFF5F5F5) // Un gris muy claro, casi blanco
val SurfaceLight = Color(0xFFFFFFFF)    // Blanco puro para las tarjetas

val BackgroundDark = Color(0xFF121212) // Un gris muy oscuro para modo oscuro
val SurfaceDark = Color(0xFF1E1E1E)    // Un gris oscuro para tarjetas en modo oscuro

// Colores de Texto y Iconos (para contraste en diferentes fondos)
val OnPrimaryLight = Color(0xFFFFFFFF) // Texto blanco sobre colores primarios claros
val OnSurfaceLight = Color(0xFF212121) // Texto oscuro sobre superficies claras

val OnPrimaryDark = Color(0xFFFFFFFF) // Texto blanco sobre colores primarios oscuros
val OnSurfaceDark = Color(0xFFE0E0E0)  // Texto claro sobre superficies oscuras

// Colores de Error
val ErrorRed = Color(0xFFEF5350)     // Rojo estándar para mensajes de error
val OnError = Color(0xFFFFFFFF)      // Texto blanco sobre rojo de error